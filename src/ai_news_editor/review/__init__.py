"""Human review: the actions a person takes on a draft.

Deliberately independent of the CLI, so the planned Telegram review bot can call the
same functions rather than reimplementing the rules behind a different button.
"""
