"""Turning shortlisted stories into Ukrainian draft posts.

The same export/validate/import pattern as the editorial exchange, one stage later. A
draft is text waiting for a human — never an approval, never a published post.
"""
